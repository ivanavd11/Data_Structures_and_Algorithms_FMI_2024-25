#include <bits/stdc++.h>
using namespace std;

int main() 
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);
    int n,k;
    cin>>n>>k;
    std::vector <int> v(n);
    for(int i=0; i<n;i++)
    {
        std::cin>>v[i];
    }
    
    for(auto j:v)
    {
        if(j!=k)
        {
            cout<<j<<" ";
        }
    }
    return 0;
}
